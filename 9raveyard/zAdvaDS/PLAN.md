get the directory

check if it exists

list of all file paths inside the directory

list of all file objects using the file paths

list of all dataframes from all file objects

cleaning

drop categorical values

drop rows with missing values

process for dataframes containing dates.

Start: Directory

End:
- Report containing key statistical insights and diagrams.
- Ready-to-use trained models based on the data.
- Forecasting based on ML models.
- Cleaned up file.

