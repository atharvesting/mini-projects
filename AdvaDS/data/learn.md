File Handling:
```python
os.path.isdir(dir_path) # Check if directory exists
list(os.walk(dir_path)) # Recursive traversal through directory
os.path.basename(file_path) # Full file name
os.path.splitext(file_path) # Returns tuple containing file name and extension
```
