#include "model.h"
#include "utils.h"
#include <iostream>

int main() {

	//Matrix<int> m(1, 5), b(5, 1);

	//m.fill_random(1, 20);
	//printMatrix(m);

	//b.fill_random(1, 20);
	//printMatrix(b);

	//auto c = m - b;
	//printMatrix(c);

	//auto d = -b;
	//printMatrixAdv(d);

	std::cout << no_of_digits(15768459.1) << std::endl;
	std::cout << 0 - 0.1 << std::endl;

	return 0;

}